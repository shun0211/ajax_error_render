source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in ajax_error_render.gemspec.
gemspec

group :development do
  gem 'mysql2', '~> 0.5'
end

group :test do
  gem 'webdrivers'
  gem 'capybara'
end

# To use a debugger
# gem 'byebug', group: [:development, :test]
